{
  "$comment": "ShowMe: エージェント設定を表示 が開く文書の日本語版（D58 / D62）。英語版は agent-config-doc.ts の english()。{0} ブリッジのパス / {1} シェル用に引用したパス / {2} 許可リスト JSON / {3} JSON 文字列として引用したパス / {4} Copilot CLI の JSON。両方の版が同じ事実を書くことは test/agent-config-doc.test.ts が両方に同じ検査を当てて確かめる。",
  "lines": [
    "# ShowMe — エージェント設定（コピーして使う。このファイルは保存しなくてよい）",
    "",
    "ShowMe は他のツールの設定ファイルを書き換えない。この文書は untitled で、",
    "必要な断片を自分のエージェントの設定に写して使う。",
    "",
    "ブリッジ: {0}",
    "",
    "## Claude Code",
    "",
    "```sh",
    "claude mcp add showme -- node {1}",
    "```",
    "",
    "許可ルール（`.claude/settings.json` の `permissions.allow` に足す。",
    "無いと「見せるだけ」のツールでも毎回確認が出る）:",
    "",
    "```json",
    "{2}",
    "```",
    "",
    "`arrange_editors` は入れていない。唯一の破壊的なツール（人間のタブを閉じうる）",
    "なので、一括の許可に混ぜない。使う場合は自分で `\"mcp__showme__arrange_editors\"` を",
    "1行足す。",
    "",
    "削除:",
    "",
    "```sh",
    "claude mcp remove showme",
    "```",
    "",
    "## Codex CLI（`~/.codex/config.toml`）",
    "",
    "```toml",
    "[mcp_servers.showme]",
    "command = \"node\"",
    "args = [{3}]",
    "```",
    "",
    "削除: 上の `[mcp_servers.showme]` の節を消す。",
    "",
    "## Copilot CLI（`~/.copilot/mcp-config.json`）",
    "",
    "```json",
    "{4}",
    "```",
    "",
    "許可: `--allow-tool 'showme'`",
    "",
    "削除: `\"showme\"` の項目を消す。",
    "",
    "## VS Code 内蔵のエージェント",
    "",
    "拡張が MCP の提供者（`showme`）を登録しているので、設定は要らない",
    "（制限モードでは効かない）。"
  ]
}
